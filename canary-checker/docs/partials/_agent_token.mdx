1. Click on **Settings** → **Agents** in the sidebar
1. <div class="flex flow-row space-x-2"><div>Click on&nbsp;</div><div class="mt-3"><img src="/img/agents-add.png" height="25px" /></div></div>
    <Screenshot img="/img/create_agent.png" size="800px"/>
1. Enter an agent name - This can a cluster name, or an account name (for hub-spoke clusters). <br/>
    > Keep the agent name short and unique as it is used as a tag in the UI.
1. Click on **Next**
1. Copy the generated values for `upstream.agentName`, `upstream.host`, `upstream.username`, `upstream.password`
{props.children}
