---
title: AWS Config
sidebar_custom_props:
  icon: aws-config
---

# <Icon name="aws-config" /> AWS Config

<!-- Source: modules/canary-checker/api/v1/checks.go#AwsConfigCheck -->

The AWS Config check runs a SQL query against AWS Config resources.


```yaml title="aws-config.yaml" file=<rootDir>/modules/canary-checker/fixtures/aws/aws_config_pass.yaml
```



<HealthCheck name="awsConfig" connection="aws" rows={[
  {
    field: "query",
    required: true,
    description: "SQL query to run against AWS Config resources",
    scheme: 'string'
  },
  {
    field: "aggregatorName",
    description: "Specify the name of the configuration aggregator",
    scheme: 'string'
  }
]}/>

