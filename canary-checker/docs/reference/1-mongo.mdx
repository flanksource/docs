---
title: MongoDB
sidebar_custom_props:
  icon: mongo
---

# <Icon name="mongo" /> MongoDB

<!-- Source: modules/canary-checker/api/v1/checks.go#MongoDBCheck -->

The MongoDB check connects to MongoDB and pings the primary to verify availability.

```yaml title="mongodb.yaml" file=<rootDir>/modules/canary-checker/fixtures/datasources/mongo_pass.yaml
```
<HealthCheck name="mongodb" connection="url" rows={[
  {field: "url", description: "MongoDB [connection string](https://www.mongodb.com/docs/manual/reference/connection-string/), such as `mongodb://:27017/?authSource=admin`. Required unless `connection` supplies the URL.", scheme: "string"},

]}/>
