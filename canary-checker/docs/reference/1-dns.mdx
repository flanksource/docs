---
title: DNS
sidebar_custom_props:
  icon: dns
---

# <Icon name="dns" /> DNS

<!-- Source: modules/canary-checker/api/v1/checks.go#DNSCheck -->

The DNS check queries DNS records and measures DNS response latency. Use this check to:

* Check the latency of the DNS server in a Kubernetes cluster.
* Verify that a DNS-based load balancer has serving records.

```yaml title="dns.yaml"  file=<rootDir>/modules/canary-checker/fixtures/minimal/dns_pass.yaml
```


<HealthCheck name="dns"  rows={[
  {
    field: "server" ,
    description: "DNS server to query. If omitted, Canary Checker uses the host resolver.",
    scheme: 'string'

  },
  {
    field: "port",
    description: "Port to query DNS server on",
    default: "53",
    scheme: "int"
  },
  {
    field: "query",
    description: "Domain name to look up",
    scheme: 'string',
    required: true
  },
  {
    field: "querytype",
    description: "Record type to query. Supported values are `A`, `CNAME`, `SRV`, `MX`, `PTR`, `TXT`, and `NS`.",
    scheme: 'string',
    default: "A",
  },
  {
    field: "exactreply",
    description: "Expected exact match result(s)",
    scheme: '[]string'
  },
  {
    field: "minrecords",
    description: "Minimum number of records expected in response",
    scheme: 'int'
  },
  {
    field: "thresholdMillis",
    description: "Threshold response time from DNS server",
    scheme: 'int'
  },
  {
    field: "timeout",
    description: "Check timeout in seconds",
    default: "10",
    scheme: 'int'
  }

]}/>
