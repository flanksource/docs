---
title: ContainerD / Docker
sidebar_class_name: beta
sidebar_custom_props:
  icon: containerd
---

# <Icon name="containerd" /> ContainerD / Docker

This check pulls a Docker image from specified registry using containerd and then verify its checksum and size.

:::note Root Privileges Required
The containerd and docker health checks require access to a working socket that is mounted at runtime
:::

## Pulling images

```yaml file=<rootDir>/modules/canary-checker/fixtures/minimal/containerd-pull.yaml title='containerd-pull.yaml'
```

<HealthCheck name="containerd"  rows={[
  {
    field: "image" ,
    description: "Full path to image, including registry",
    scheme: 'string',
    required: true
  },
  {
    field: "expectedDigest",
    description: "Expected digest of the pulled image",
    scheme: 'string',
  },
  {
    field: "expectedSize",
    description: "Expected size of the pulled image",
    scheme: 'int64',
  },

    {
    field: "username",
    description: "Registry username for authentication",
    scheme: 'string',
  },
      {
    field: "password",
    description: "Registry password for authentication",
    scheme: 'string',
  },
]}/>


---

## Pushing images

This check pushes a Docker image to a specified registry using containerd.

```yaml file=<rootDir>/modules/canary-checker/fixtures/minimal/containerd-push.yaml title='containerd-push.yaml'
```


<HealthCheck name="containerdPush"  rows={[
  {
    field: "image" ,
    description: "Full path to image, including registry",
    scheme: 'string',
    required: true
  },


    {
    field: "username",
    description: "Registry username for authentication",
    scheme: 'string',
    required: true
  },
      {
    field: "password",
    description: "Registry password for authentication",
    scheme: 'string',
       required: true
  },
]}/>
