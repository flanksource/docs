---
title: TCP
sidebar_custom_props:
  icon: network
---

# <Icon name="network" /> TCP

<!-- Source: modules/canary-checker/api/v1/checks.go#TCPCheck -->

The TCP check verifies that a network port is reachable.

```yaml title="tcp.yaml" file=<rootDir>/modules/canary-checker/fixtures/minimal/tcp.yaml
```

<HealthCheck name="tcp" rows={[
  {field: "endpoint", description: "Hostname or IPv4 address and port in `HOST:PORT` format. Do not include a URL scheme or path. IPv6 literals are not supported", scheme: 'string', required: true},
  {
    field: "thresholdMillis",
    description: "Connection timeout in milliseconds. When unset or `0`, the check does not set an explicit timeout",
    scheme: 'int64'
  }

]}/>
