---
title: Azure DevOps
sidebar_custom_props:
  icon: azure-devops
---

# <Icon name="azure-devops" /> Azure DevOps

<!-- Source: modules/canary-checker/api/v1/checks.go#AzureDevopsCheck -->

The Azure DevOps check verifies that the latest completed matching pipeline run succeeded.


```yaml title="azure-devops.yaml" file=<rootDir>/modules/canary-checker/fixtures/azure/devops.yaml
```

<HealthCheck name="azureDevops" edition="standard" rows={[
  {
    field: "connection",
    description: "Connection URL for Azure DevOps credentials, such as `connection://namespace/name`. Use either `connection` or `personalAccessToken`.",
    scheme: "string"
  },
  {
    field: "organization",
    description: "Azure DevOps organization",
    required: true
  }, {
    field: "project",
    description: "Project name or ID",
    required: true
  }, {
    field: "pipeline",
    description: "Pipeline name or regular expression to match",
    required: true
  },

  {
    field: "variables",
    description: "Only match runs with these variable values",
    scheme: "map[string]string"
  },
  {
    field: "branch",
    description: "Only match runs from these branches",
    scheme: "[]string"
  },
  {
    field: "thresholdMillis",
    description: "Maximum run duration in milliseconds before the check is unhealthy",
    scheme: 'int'
  },
  {
    field: "personalAccessToken",
    description: "Azure DevOps personal access token. Use either `personalAccessToken` or `connection`.",
    scheme: "EnvVar",
    required: true
  }

]}/>
