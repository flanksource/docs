---
title: AWS Cloud Watch
hide_title: true
sidebar_custom_props:
  icon: aws-cloudwatch
---

# <Icon name="aws-cloudwatch" size={32}> CloudWatch</Icon>

<!-- Source: modules/canary-checker/api/v1/checks.go#CloudWatchCheck -->

The CloudWatch check monitors the status of CloudWatch alarms in AWS. It can filter alarms by various criteria and supports transforming alarm data for custom health checks.

```yaml title="cloudwatch.yaml"  file=<rootDir>/modules/canary-checker/fixtures/aws/cloudwatch_pass.yaml
```

<HealthCheck name="cloudwatch" connection="aws" rows={[
  {
    field: "actionPrefix",
    description: "Filter alarms that use a specific action prefix. For example, specify the ARN of an SNS topic to find all alarms that send notifications to that topic",
    scheme: "*string"
  },
  {
    field: "alarmPrefix",
    description: "Filter alarms whose names start with this prefix",
    scheme: "*string"
  },
  {
    field: "alarms",
    description: "List of specific alarm names to check",
    scheme: "[]string"
  },
  {
    field: "state",
    description: "Filter alarms by state value (e.g., 'OK', 'ALARM', 'INSUFFICIENT_DATA')",
    scheme: "string"
  }
]}/>


