---
title: S3 Protocol
sidebar_className: infra
sidebar_custom_props:
  icon: logos:aws-s3
tags:
- enterprise
---

# <Icon name="aws-s3" /> S3 Protocol

<!-- Source: modules/canary-checker/api/v1/checks.go:205#S3Check -->

Checks if S3 compatible endpoints (like Minio, EMC ECS) are functioning correctly,

:::info
To check the contents of a S3 bucket use: [S3 Bucket](folder#s3)
:::

The S3 check:

- Lists objects in the bucket to check for Read permissions
- Puts an object into the bucket for Write permissions
- Downloads previously uploaded object to check for Get permissions


```yaml title="s3-protocol"  file=<rootDir>/modules/canary-checker/fixtures/aws/s3-protocol.yaml
```
<HealthCheck name="s3" edition="enterprise" connection="aws" rows={[

  {field: "bucketName", description: "Bucket name to test against", scheme: "string", required: true},
  {field: "storageClass", description: "Storage Class of the test object to create", scheme: "string"},
  {field: "objectPath", description: "Glob path to restrict matches to a subset", scheme: "string"},
  {field: "usePathStyle", description: "Use path style path: http://s3.amazonaws.com/BUCKET/KEY instead of http://BUCKET.s3.amazonaws.com/KEY", scheme: "boolean"},

]}/>


