---
title: Helm
sidebar_custom_props:
  icon: helm
---

import { Card, Cards } from '@site/../common/src/components/Card'
import Helm from '@site/../common/src/components/Helm.jsx'
import Schema from '@site/modules/mission-control-registry/charts/helm/values.schema.json'
import Prereqs from '@site/docs/partials/_integration_prereqs.mdx'
import { CapabilityBadges, CapabilityBadge, CapabilityHeading } from '@site/../common/src/components/CapabilityBadges'

export const toc = [
  { value: 'Health Checks', id: 'health-checks', level: 2 },
  { value: 'Playbooks', id: 'playbooks', level: 2 },
];

<CapabilityBadges>
  <CapabilityBadge type="healthcheck" />
  <CapabilityBadge type="playbook" />
</CapabilityBadges>

Mission Control integrates with Helm to manage chart deployments across clusters.

## Quick Start

<Prereqs />

FluxCD must be running in the cluster for Helm release management.

Install the [mission-control-helm](https://artifacthub.io/packages/helm/flanksource/mission-control-helm) chart:

<Helm chart="mission-control-helm"
  createRepo={false}
  schema={Schema}
  createNamespace={false}
  values={{
    "playbook": {
      "enabled": true
    },
    "scraper": {
      "charts": [
        "bitnami/kafka",
        "bitnami/postgresql",
        "bitnami/rabbitmq",
        "prometheus-community/prometheus"
      ]
    }
  }}/>

---

<CapabilityHeading type="healthcheck" />

**Use cases:**
- Discover Helm releases and track their versions, values, and upgrade availability
- Monitor release health and correlate failures with recent value changes
- Verify chart repository access and authentication before deployments fail

### Example

Verify Helm chart repository availability and authentication before deployments fail in production.

```yaml file=<rootDir>/modules/canary-checker/fixtures/k8s/helm_check.yaml title='helm-health-check.yaml'
```

---

<CapabilityHeading type="playbook" />

**Use cases:**
- Deploy new charts or update existing releases through playbooks
- Link Helm releases to the Kubernetes resources they create

## Installing Charts with Playbook

The `Install Helm Chart` playbook installs charts into any Kubernetes namespace with user-provided values:

<Screenshot img="/img/helm-integration-playbook.png" />

Additional playbooks for Helm operations:

- **Update Chart Version**: Upgrade to a newer chart version
- **Update Values**: Modify Helm values for an existing release

The [Kubernetes](/docs/integrations/kubernetes/playbooks) and [Flux](/docs/integrations/flux/playbooks) playbooks provide further operations on charts and the resources they create.

## Next Steps

<Cards>
  <Card icon="flux" title="Flux Integration" href="/docs/integrations/flux">
    Manage HelmReleases via Flux GitOps
  </Card>
  <Card icon="kustomize" title="Kustomize Integration" href="/docs/integrations/kustomize">
    Create HelmReleases through Kustomize
  </Card>
  <Card icon="k8s" title="Kubernetes Integration" href="/docs/integrations/kubernetes">
    Kubernetes resource monitoring
  </Card>
</Cards>
