---
title: [Platform Name]
sidebar_custom_props:
  icon: [icon-name]
# draft: true  # Uncomment if page is not ready for production
---

import { Card, Cards } from '@site/../common/src/components/Card'
import Helm from '@site/../common/src/components/Helm.jsx'
import { CapabilityBadges, CapabilityBadge, CapabilityHeading } from '@site/../common/src/components/CapabilityBadges'

export const toc = [
  { value: 'Catalog', id: 'catalog', level: 2 },
  { value: 'Health Checks', id: 'health-checks', level: 2 },
  { value: 'Playbooks', id: 'playbooks', level: 2 },
  { value: 'Notifications', id: 'notifications', level: 2 },
];

<CapabilityBadges>
  <CapabilityBadge type="scraper" />
  <CapabilityBadge type="healthcheck" />
  <CapabilityBadge type="playbook" />
  <CapabilityBadge type="notifications" />
</CapabilityBadges>

[Brief 2-3 sentence description of what Mission Control can do with this platform.]

:::info Prerequisites
- Mission Control installed via Helm
- [Platform-specific requirements, e.g., API keys, credentials, access rights]
:::

<Helm chart="mission-control" namespace="mission-control" />

---

<CapabilityHeading type="scraper" />

Discover and track [Platform] configuration changes in the Catalog.

**Use cases:**
- [Use case 1, e.g., Track EC2 instance configurations across regions]
- [Use case 2, e.g., Monitor S3 bucket policies for compliance]
- [Use case 3, e.g., Correlate resources with Terraform state]

### Example

```yaml title="[platform]-scraper.yaml"
apiVersion: configs.flanksource.com/v1
kind: ScrapeConfig
metadata:
  name: [platform]-scraper
spec:
  schedule: "@every 5m"
  [platform]:
    - # Add minimal working scraper configuration
```

<Cards>
  <Card title="Scraper Reference" link="/docs/guide/config-db/scrapers/[platform]" />
</Cards>

---

<CapabilityHeading type="healthcheck" />

Monitor [Platform] resources and services with health checks.

**Use cases:**
- [Use case 1, e.g., Verify API endpoint availability]
- [Use case 2, e.g., Check database connectivity]
- [Use case 3, e.g., Monitor queue depths and latency]

### Example

```yaml title="[platform]-health-check.yaml"
apiVersion: canaries.flanksource.com/v1
kind: Canary
metadata:
  name: [platform]-check
spec:
  interval: 30
  [platform]:
    - name: sample-check
      # Add minimal working configuration
```

<Cards>
  <Card title="Health Check Reference" link="/docs/guide/canary-checker/reference/[platform]" />
</Cards>

---

<CapabilityHeading type="playbook" />

Automate remediation and operations for [Platform] resources.

**Use cases:**
- [Use case 1, e.g., Auto-restart failed services]
- [Use case 2, e.g., Scale resources based on metrics]
- [Use case 3, e.g., Create PRs to update configurations]

### Example

```yaml title="[platform]-playbook.yaml"
apiVersion: mission-control.flanksource.com/v1
kind: Playbook
metadata:
  name: [platform]-remediation
spec:
  on:
    canary:
      - name: [platform]-check
        failed: true
  actions:
    - name: remediate
      # Add playbook action
```

<Cards>
  <Card title="Playbook Actions" link="/docs/guide/playbooks/actions" />
</Cards>

---

<CapabilityHeading type="notifications" />

Send alerts and notifications via [Platform].

**Use cases:**
- [Use case 1, e.g., Alert on-call when health checks fail]
- [Use case 2, e.g., Notify team channel of config changes]
- [Use case 3, e.g., Create incidents for critical issues]

### Example

```yaml title="notification.yaml"
apiVersion: mission-control.flanksource.com/v1
kind: Notification
metadata:
  name: [platform]-alerts
spec:
  to:
    [platform]: [connection-uri]
  events:
    - incident.created
    - check.failed
```

<Cards>
  <Card title="Notification Channels" link="/docs/guide/notifications/channels/[platform]" />
</Cards>

---

## Next Steps

<Cards>
  <Card title="[Platform] Examples" link="/docs/integrations/[platform]/examples" />
  <Card title="Connection Reference" link="/docs/reference/connections/[platform]" />
</Cards>
