---
title: Pushbullet
---

```yaml
apiVersion: mission-control.flanksource.com/v1
kind: Connection
metadata:
  name: pushbullet-connection
  namespace: default
spec:
  pushbullet:
    token:
      value: your-pushbullet-token
    targets:
      - target1
      - target2
```

<Fields rows={[
  {
    field: 'token',
    scheme: 'envvar',
    required: true
  },
  {
    field: 'targets',
    scheme: 'string[]',
    required: true,
  },
]}/>
