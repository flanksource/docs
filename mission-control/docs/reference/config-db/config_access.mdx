---
title: Config Access
sidebar_custom_props:
  icon: lock
sidebar_position: 2
---

import EntityModelDiagram from '@site/../common/src/components/EntityModelDiagram'

Config Access tracks who has access to configuration items and their access history. This enables compliance auditing, security monitoring, and periodic access reviews.

## Entity Model

<EntityModelDiagram className="mx-auto overflow-x-auto" />

## User

Users represent individuals or service accounts from your identity providers or external systems.

<Fields rows={[
  {
    field: "id",
    description: "Unique identifier (auto-generated)",
    scheme: "uuid"
  },
  {
    field: "name",
    description: "Display name of the user",
    scheme: "string",
    required: true
  },
  {
    field: "account_id",
    description: "Account identifier from the source system. Auto-populated during scraping if not provided",
    scheme: "string"
  },
  {
    field: "user_type",
    description: "Type of user: `human`, `service`, or `system`",
    scheme: "string"
  },
  {
    field: "email",
    description: "User's email address",
    scheme: "string"
  },
  {
    field: "aliases",
    description: "Alternative identifiers for alias resolution (e.g. `userPrincipalName`, `mail`, `onPremisesSamAccountName`)",
    scheme: "[]string"
  },
  {
    field: "scraper_id",
    description: "ID of the scraper that created this user. Auto-populated from scraper context",
    scheme: "uuid"
  }
]}/>

## Group

Groups represent teams, departments, or security groups from your identity systems.

<Fields rows={[
  {
    field: "id",
    description: "Unique identifier (auto-generated)",
    scheme: "uuid"
  },
  {
    field: "name",
    description: "Group name",
    scheme: "string",
    required: true
  },
  {
    field: "account_id",
    description: "Group identifier from the source system",
    scheme: "string",
    required: true
  },
  {
    field: "group_type",
    description: "Type of group: `team`, `role`, `department`, or `security`",
    scheme: "string"
  },
  {
    field: "aliases",
    description: "Alternative identifiers for alias resolution",
    scheme: "[]string"
  },
  {
    field: "scraper_id",
    description: "ID of the scraper that created this group",
    scheme: "uuid",
    required: true
  }
]}/>

## Role

Roles define permission sets that can be assigned to users or groups.

<Fields rows={[
  {
    field: "id",
    description: "Unique identifier (auto-generated)",
    scheme: "uuid"
  },
  {
    field: "name",
    description: "Role name",
    scheme: "string",
    required: true
  },
  {
    field: "account_id",
    description: "Role identifier from the source system",
    scheme: "string",
    required: true
  },
  {
    field: "role_type",
    description: "Type of role: `builtin`, `custom`, `database`, or `application`",
    scheme: "string"
  },
  {
    field: "description",
    description: "Human-readable description of the role's purpose",
    scheme: "string"
  },
  {
    field: "aliases",
    description: "Alternative identifiers for alias resolution",
    scheme: "[]string"
  },
  {
    field: "application_id",
    description: "ID of the Application this role belongs to",
    scheme: "uuid"
  },
  {
    field: "scraper_id",
    description: "ID of the scraper that created this role",
    scheme: "uuid"
  }
]}/>

## User Group

Links users to their group memberships.

<Fields rows={[
  {
    field: "external_user_id",
    description: "ID of the external user (resolved from `user` alias during scraping)",
    scheme: "uuid",
    required: true
  },
  {
    field: "external_group_id",
    description: "ID of the external group the user belongs to (resolved from `group` alias during scraping)",
    scheme: "uuid",
    required: true
  }
]}/>

## Config Access

Records which users, groups, and roles have access to specific configuration items. At least one of `external_user_id`, `external_group_id`, or `external_role_id` (or their shorthand equivalents `user`, `role`, `group`) must be set.

At least one of `scraper_id`, `application_id`, or `source` must be set. When run via a scraper, `scraper_id` is auto-populated from context.

<Fields rows={[
  {
    field: "id",
    description: "Unique identifier. Auto-generated from a deterministic hash of `(config_id, external_user_id, external_role_id, external_group_id, scraper_id)` if not provided",
    scheme: "string"
  },
  {
    field: "config_id",
    description: "ID of the config item being accessed. Accepts a UUID (used directly) or a non-UUID string (treated as an external ID and resolved via lookup with `config_type`)",
    scheme: "uuid",
    required: true
  },
  {
    field: "external_config_id",
    description: "External config reference for lookup when `config_id` is not a UUID",
    scheme: "[ExternalID](#externalid)"
  },
  {
    field: "external_user_id",
    description: "ID of the external user with access",
    scheme: "uuid"
  },
  {
    field: "external_user_aliases",
    description: "Aliases used to resolve the user. Populated by the `user` shorthand",
    scheme: "[]string"
  },
  {
    field: "external_group_id",
    description: "ID of the external group with access",
    scheme: "uuid"
  },
  {
    field: "external_group_aliases",
    description: "Aliases used to resolve the group. Populated by the `group` shorthand",
    scheme: "[]string"
  },
  {
    field: "external_role_id",
    description: "ID of the external role granting access",
    scheme: "uuid"
  },
  {
    field: "external_role_aliases",
    description: "Aliases used to resolve the role. Populated by the `role` shorthand",
    scheme: "[]string"
  },
  {
    field: "application_id",
    description: "ID of the Application this access record belongs to",
    scheme: "uuid"
  },
  {
    field: "scraper_id",
    description: "ID of the scraper that created this record (auto-populated from scraper context)",
    scheme: "uuid"
  },
  {
    field: "source",
    description: "Source identifier for this access record",
    scheme: "string"
  },
  {
    field: "last_reviewed_at",
    description: "Timestamp of the last access review",
    scheme: "timestamp"
  },
  {
    field: "last_reviewed_by",
    description: "User who performed the last access review",
    scheme: "uuid"
  }
]}/>

### Input Shorthands

These fields are accepted in scraper input and resolved during processing:

| Shorthand | Resolves To | Description |
|-----------|-------------|-------------|
| `user` | `external_user_aliases` | Matched against user aliases |
| `role` | `external_role_aliases` | Matched against role aliases |
| `group` | `external_group_aliases` | Matched against group aliases |
| `config_id` (non-UUID) | `external_config_id.external_id` | External ID for config lookup |
| `config_type` / `type` | `external_config_id.config_type` | Config type for external ID lookup |

### ExternalID {#externalid}

<Fields rows={[
  {
    field: "config_id",
    description: "Config ID (UUID string)",
    scheme: "string"
  },
  {
    field: "config_type",
    description: "Config type for external ID lookup",
    scheme: "string"
  },
  {
    field: "external_id",
    description: "External identifier for the config item",
    scheme: "string"
  },
  {
    field: "scraper_id",
    description: "Scraper ID to scope the lookup",
    scheme: "string"
  }
]}/>

## Config Access Log

Records individual access events for audit purposes. Deduplicated by `(config_id, external_user_id, scraper_id)` — on conflict, `count` is incremented and `created_at`, `mfa`, and `properties` are updated only if the new record has a more recent `created_at`.

<Fields rows={[
  {
    field: "config_id",
    description: "ID of the config item that was accessed. Accepts a UUID or a non-UUID string (resolved via external ID lookup with `config_type`)",
    scheme: "uuid",
    required: true
  },
  {
    field: "external_config_id",
    description: "External config reference for lookup when `config_id` is not a UUID",
    scheme: "[ExternalID](#externalid)"
  },
  {
    field: "external_user_id",
    description: "ID of the external user who accessed the resource",
    scheme: "uuid",
    required: true
  },
  {
    field: "external_user_aliases",
    description: "Aliases used to resolve the user. Populated by the `user` shorthand",
    scheme: "[]string"
  },
  {
    field: "scraper_id",
    description: "ID of the scraper that captured this access log (auto-populated from scraper context)",
    scheme: "uuid",
    required: true
  },
  {
    field: "created_at",
    description: "Timestamp when the access occurred",
    scheme: "timestamp"
  },
  {
    field: "mfa",
    description: "Whether multi-factor authentication was used",
    scheme: "bool"
  },
  {
    field: "count",
    description: "Number of aggregated access events (default: 1)",
    scheme: "int"
  },
  {
    field: "properties",
    description: "Additional access metadata (IP address, session info, client, etc.)",
    scheme: "map[string]any"
  }
]}/>

### Input Shorthands

| Shorthand | Resolves To | Description |
|-----------|-------------|-------------|
| `user` | `external_user_aliases` | Matched against user aliases |
| `config_id` (non-UUID) | `external_config_id.external_id` | External ID for config lookup |
| `config_type` / `type` | `external_config_id.config_type` | Config type for external ID lookup |
