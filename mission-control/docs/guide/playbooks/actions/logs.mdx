---
title: Logs
sidebar_custom_props:
  icon: logs

---
import Templating from "@site/docs/reference/playbooks/context.mdx"

<!-- Source: modules/mission-control/api/v1/playbook_actions.go#LogsAction -->

# <Icon name="logs"/> Logs Action

The Logs action fetches and queries logs from various backends. Use it to retrieve logs for debugging, analysis, or to pass to downstream actions like AI analysis.

```yaml title="kubernetes-logs.yaml" file=<rootDir>/modules/mission-control/fixtures/playbooks/logs/k8s.yaml

```

<Action rows={[
    {field: "logs", description: "Logs Action", scheme: "[Logs](#logs)"},
]} />

## Logs

Specify exactly one backend. All of the [post processing](#post-processing) fields are set inside the backend, alongside its query.

<Fields oneOf={["loki", "cloudwatch", "opensearch", "kubernetes"]} rows={[
  {field: "cloudwatch", description: "Query logs from AWS CloudWatch Logs", scheme: "[CloudWatch](#cloudwatch)"},
  {field: "kubernetes", description: "Fetch logs directly from Kubernetes pods", scheme: "[Kubernetes](#kubernetes)"},
  {field: "loki", description: "Query logs from Grafana Loki", scheme: "[Loki](#loki)"},
  {field: "opensearch", description: "Query logs from OpenSearch/Elasticsearch", scheme: "[OpenSearch](#opensearch)"}
]}/>

### Post processing

Every backend accepts the following fields to filter, parse and deduplicate logs after they have been retrieved.

<Fields rows={[
  {field: "dedupe.window", description: "Time window for deduplication (e.g., `5m`, `1h`). Logs with identical dedupe fields within this window are merged", scheme: "duration"},
  {field: "dedupe.fields", description: "Fields to use for identifying duplicate log entries (e.g., `['message', 'level']`). Two logs that have an empty value for a field are still deduped", scheme: "[]string"},
  {field: "match", description: "CEL expressions to filter logs after retrieval. A log is kept if any of the expressions match", scheme: "[[]MatchExpression](/docs/reference/types#match-expression)"},
  {field: "parse", description: "Log format to parse the message with", scheme: "`logfmt` | `klogfmt` | `json` | `syslog` | `autodetect`"},
  {field: "mapping.id", description: "Source field names for the unique log identifier", scheme: "[]string"},
  {field: "mapping.message", description: "Source field names for the log message content", scheme: "[]string"},
  {field: "mapping.timestamp", description: "Source field names for the timestamp (tries each until non-empty)", scheme: "[]string"},
  {field: "mapping.severity", description: "Source field names for the log level/severity", scheme: "[]string"},
  {field: "mapping.host", description: "Source field names for the host", scheme: "[]string"},
  {field: "mapping.source", description: "Source field names for the log source", scheme: "[]string"},
  {field: "mapping.ignore", description: "Fields to drop from the log labels", scheme: "[]string"},
  {field: "mapping.groupBy", description: "Fields to group the returned logs by", scheme: "[]string"},
  {field: "mapping.dedupBy", description: "Fields to dedupe the returned logs by", scheme: "[]string"}
]}/>

### Time range

The `cloudwatch`, `kubernetes` and `loki` backends share a common time range and limit.

<Fields rows={[
  {field: "start", description: "Start time of the query. Supports [datemath](https://opensearch.org/docs/latest/field-types/supported-field-types/date/#date-math) e.g. `now-24h`", scheme: "string"},
  {field: "end", description: "End time of the query. Defaults to now", scheme: "string"},
  {field: "limit", description: "Maximum number of log lines to return", scheme: "string", templatable: true}
]}/>

### CloudWatch

Query logs from [AWS CloudWatch Logs](https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/) using CloudWatch Logs Insights.

<Fields rows={[
  {field: "connection", description: "AWS connection for credentials", scheme: "[AWSConnection](/docs/reference/connections/aws)"},
  {field: "accessKey", description: "AWS access key ID", scheme: "[EnvVar](/docs/reference/env-var)"},
  {field: "secretKey", description: "AWS secret access key", scheme: "[EnvVar](/docs/reference/env-var)"},
  {field: "sessionToken", description: "AWS session token", scheme: "[EnvVar](/docs/reference/env-var)"},
  {field: "assumeRole", description: "ARN of the role to assume", scheme: "string"},
  {field: "region", description: "AWS region (e.g., `us-east-1`)", scheme: "string"},
  {field: "endpoint", description: "Custom AWS endpoint", scheme: "string"},
  {field: "skipTLSVerify", description: "Skip TLS verification when connecting to AWS", scheme: "bool"},
  {field: "logGroup", description: "CloudWatch log group to query", scheme: "string", required: true, templatable: true},
  {field: "query", description: "CloudWatch Logs Insights query to run on the log group", scheme: "string", required: true, templatable: true}
]}/>

<details summary="Example">

```yaml title="cloudwatch.yaml" file=<rootDir>/modules/mission-control/fixtures/playbooks/logs/cloudwatch.yaml

```

</details>

### Kubernetes

Fetch logs directly from Kubernetes pods using the Kubernetes API. Logs are fetched for the resource identified by `kind`, `apiVersion`, `namespace` and `name` — for a workload such as a Deployment, logs from all of its pods are returned.

<Fields rows={[
  {field: "connection", description: "Kubernetes connection for cluster access", scheme: "[KubernetesConnection](/docs/reference/connections/kubernetes)"},
  {field: "kubeconfig", description: "Kubeconfig content or path", scheme: "[EnvVar](/docs/reference/env-var)"},
  {field: "kind", description: "Kind of the resource to fetch logs for e.g. `Pod`, `Deployment`, `StatefulSet`, `DaemonSet`", scheme: "string", templatable: true},
  {field: "apiVersion", description: "API version of the resource", scheme: "string", templatable: true},
  {field: "namespace", description: "Namespace of the resource", scheme: "string", templatable: true},
  {field: "name", description: "Name of the resource", scheme: "string", templatable: true},
  {field: "pods", description: "Include pods matching any of these selectors. Applies when fetching logs at a higher resource level, such as a deployment spanning multiple pods", scheme: "[[]ResourceSelector](/docs/reference/resource-selector)"},
  {field: "containers", description: "Fetch logs from only these containers", scheme: "[[]MatchExpression](/docs/reference/types#match-expression)"}
]}/>

<details summary="Example">

```yaml title="k8s.yaml" file=<rootDir>/modules/mission-control/fixtures/playbooks/logs/k8s.yaml

```

</details>

### Loki

Query logs from [Grafana Loki](https://grafana.com/oss/loki/) using LogQL.

<Fields rows={[
  {field: "connection", description: "Connection name for Loki credentials", scheme: "[Connection](/docs/reference/connections/http)"},
  {field: "url", description: "Loki server URL (e.g., `http://loki:3100`)", scheme: "string"},
  {field: "username", description: "Basic auth username", scheme: "[EnvVar](/docs/reference/env-var)"},
  {field: "password", description: "Basic auth password", scheme: "[EnvVar](/docs/reference/env-var)"},
  {field: "query", description: "LogQL query (e.g., `{app=\"myservice\"} |= \"error\"`)", scheme: "string", required: true, templatable: true},
  {field: "since", description: "Duration used to calculate `start` relative to `end`. Any value set for `start` supersedes this", scheme: "string"},
  {field: "step", description: "Query resolution step width, as a duration or a number of seconds", scheme: "string"},
  {field: "interval", description: "Only return entries at or greater than this interval", scheme: "string"},
  {field: "direction", description: "Query direction: `forward` (oldest first) or `backward` (newest first)", scheme: "`forward` | `backward`"}
]}/>

<details summary="Example">

```yaml title="loki.yaml" file=<rootDir>/modules/mission-control/fixtures/playbooks/logs/loki.yaml

```

</details>

### OpenSearch

Query logs from [OpenSearch](https://opensearch.org/) or Elasticsearch clusters.

<Fields rows={[
  {field: "connection", description: "Connection name for OpenSearch credentials", scheme: "[Connection](/docs/reference/connections/opensearch)"},
  {field: "address", description: "OpenSearch cluster address", scheme: "string"},
  {field: "username", description: "Basic auth username", scheme: "[EnvVar](/docs/reference/env-var)"},
  {field: "password", description: "Basic auth password", scheme: "[EnvVar](/docs/reference/env-var)"},
  {field: "index", description: "Index name or pattern (e.g., `logs-*`, `app-logs-2024.01.*`)", scheme: "string", required: true, templatable: true},
  {field: "query", description: "OpenSearch query DSL", scheme: "string", required: true, templatable: true},
  {field: "limit", description: "Maximum number of documents to return", scheme: "string", templatable: true}
]}/>

<details summary="Example">

```yaml title="opensearch.yaml" file=<rootDir>/modules/mission-control/fixtures/playbooks/logs/opensearch.yaml

```

</details>

## Output

The action returns a structured result containing the retrieved logs.

<Fields rows={[
  {field: "logs", description: "Log entries with normalized fields", scheme: "[[]LogLine](#log-line)"},
  {field: "groups", description: "Logs grouped by `mapping.groupBy`, when set", scheme: "[]LogGroup"},
  {field: "metadata", description: "Backend specific metadata about the query", scheme: "map[string]any"}
]}/>

### Log line

<Fields rows={[
  {field: "id", description: "Unique identifier of the log line", scheme: "string"},
  {field: "message", description: "Log message", scheme: "string"},
  {field: "severity", description: "Log level", scheme: "string"},
  {field: "firstObserved", description: "Time the log line was first seen", scheme: "time"},
  {field: "lastObserved", description: "Time the log line was last seen. Set when the line has been deduped", scheme: "time"},
  {field: "count", description: "Number of occurrences the line was deduped from", scheme: "int"},
  {field: "hash", description: "Hash of the tokenized message, used to group similar lines", scheme: "string"},
  {field: "host", description: "Host the log originated from", scheme: "string"},
  {field: "source", description: "Source of the log", scheme: "string"},
  {field: "labels", description: "Remaining fields from the log line", scheme: "map[string]string"}
]}/>

### Using Results in Subsequent Actions

```yaml
actions:
  - name: fetch-logs
    logs:
      kubernetes:
        kind: Deployment
        apiVersion: apps/v1
        namespace: production
        name: api
        start: now-1h
  - name: analyze
    if: 'size(getAction("fetch-logs").result.logs) > 0'
    ai:
      prompt: |
        Analyze these log entries:
        {{.actions.fetch-logs.result.logs | toJSON}}
```

## Templating

<Templating/>
