---
title: HTTP
sidebar_custom_props:
  icon: http

---
import Templating from "@site/docs/reference/playbooks/context.mdx"

<!-- Source: modules/mission-control/api/v1/playbook_actions.go#HTTPAction -->

# <Icon name="http"/> HTTP Action

HTTP action makes an HTTP request.

```yaml title=http..yaml"  file=<rootDir>/modules/mission-control/fixtures/playbooks/http.yaml
```

<Action rows={[
    {field: "http", description: "HTTP Action", scheme: "[HTTP](#http)"},
]} />

## HTTP

<Fields rows={[
  {field: "url", description: "Url to make the request to", scheme: "string", required: true},
  {field: "method", description: "HTTP method to use", scheme: "string", required: true, default: "GET"},
  {field: "headers", description: "Header fields to be used in the request", scheme: "[]EnvVar"},
  {field: "body", description: "Request Body Contents", scheme: "string", templatable: true},
  {field: "templatedBody", description: "When set to true the request body is templated", scheme: "bool"},
  {field: "connection", description: "Connection name. e.g. connection://http/google", scheme: "Connection"},
  {field: "username", description: "Username to authenticate with", scheme: "EnvVar"},
  {field: "password", description: "Password to authenticate with", scheme: "EnvVar"},
]}/>

## Output

<Fields  rows={[
  {field: "body", description: "Response Body Contents", scheme: "string"},
  {field: "headers", description: "Response headers", scheme: "map[string]string"},
  {field: "statusCode", description: "Response status code", scheme: "int"},
]}/>

## Templating

<Templating/>
