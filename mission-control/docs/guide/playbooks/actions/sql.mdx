---
title: SQL
sidebar_custom_props:
  icon: sql

---
import Templating from "@site/docs/reference/playbooks/context.mdx"

<!-- Source: modules/mission-control/api/v1/playbook_actions.go#SQLAction -->

# <Icon name="sql"/> SQL Action

SQL action makes a SQL query on the provided database.

```yaml title="delete-check-statuses.yaml" file=<rootDir>/modules/mission-control/fixtures/playbooks/sql.yaml
```

<Action rows={[
    {field: "sql", description: "SQL Action", scheme: "[SQL](#sql)"},
]} />

## SQL

<Fields oneOf={["connection", "url"]} rows={[

  {field: "connection", description: "Connection path  e.g. `connection://postgres/flanksource`", scheme: "Connection"},
  {field: "url", description: "Database connection URL", scheme: "url"},
  {field: "query", description: "SQL query to run against the database", scheme: "string", required: true, templatable: true},
  {field: "driver", description: "Database driver. Defaults to `postgres`", scheme: "`postgres`, `mysql`, `mssql`"},
]}/>

## Output

<Fields  rows={[
  {field: "rows", description: "List of rows with column name as keys and column data as values", scheme: "[]map[string]any"},
  {field: "count", description: "Number of rows returned", scheme: "int"},
  {field: "columns", description: "Column names, in the order returned by the query", scheme: "[]string"},
  {field: "query", description: "The query that was run", scheme: "string"},
]}/>


## Templating

<Templating/>
