---
title: Kubernetes File
sidebar_position: 12
sidebar_custom_props:
  icon: k8s-pod
---

# <Icon name="k8s-pod"/> Kubernetes File

<!-- Source: modules/config-db/api/v1/kubernetes.go#KubernetesFile -->

The KubernetesFile config type is used to scrape the contents of files inside Kubernetes Pods.

```yaml title='k8s-file-scraper.yaml' file=<rootDir>/modules/config-db/fixtures/kubernetes_file.yaml
```

## Scraper

| Field            | Description                                                                        | Scheme                                       | Required |
| ---------------- | ---------------------------------------------------------------------------------- | -------------------------------------------- | -------- |
| `logLevel`       | Specify the level of logging.                                                      | `string`                                     |          |
| `schedule`       | Specify the interval to scrape in cron format. Defaults to every 60 minutes.       | `string`                                     |          |
| `full`           | Set to `true` to extract changes from scraped configurations. Defaults to `false`. | `bool`                                       |          |
| `retention`      | Settings for retaining changes, analysis and scraped items                         | [`Retention`](/docs/guide/config-db/concepts/retention) |          |
| `kubernetesFile` | Specifies the list of Kubernetes File configurations to scrape.                    | [`[]KubernetesFile`](#kubernetesfile)        |          |



### KubernetesFile

<CustomScraper connection="kubernetes" rows={[
  {
    "field": "selector",
    "description": "Pods to extract files from",
    "scheme": "ResourceSelector",
    "required": true,
  },
  {
    "field": "container",
    "description": "Name of container",
  },
  {
    "field": "files",
    "description": "List of files inside the pod to scrape",
    "scheme": "[[]File](#file)",
    "required": "true"
  },
]}/>


### Selector

| Field           | Description                                        | Scheme   | Required |
| --------------- | -------------------------------------------------- | -------- | -------- |
| `namespace`     | Namespace of the pods                              | `string` |          |
| `kind`          | Kind of the resource to select pods from           | `string` |          |
| `name`          | Name of the resource                               | `string` |          |
| `labelSelector` | Select pods by labels e.g. `app=nginx`             | `string` |          |
| `fieldSelector` | Select pods by fields e.g. `status.phase=Running`  | `string` |          |

### File

| Field    | Description                       | Scheme     | Required |
| -------- | --------------------------------- | ---------- | -------- |
| `path`   | Set path to file contained in Pod | `[]string` |          |
| `format` | Specify format of file            | `string`   |          |

